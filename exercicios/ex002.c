// Soma de dois Números.
#include<stdio.h>
int main(){

    // Atribuindo valores as variáveis.
    int primeiro_numero = 50;
    int segundo_numero = 50;

    // Calculando os valores.
    int soma_primeiro_segundo = primeiro_numero + segundo_numero;

    // Mostrando resultado para o usuário.
    printf("%d", soma_primeiro_segundo);

return 0;
}